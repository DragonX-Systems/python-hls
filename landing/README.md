# Python-HLS Landing Page

Marketing/landing page for the Python-HLS project.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:5173

## Build for production

```bash
npm run build
```

Output in `dist/`. Can be served as static files or deployed to any static host (Vercel, Netlify, GitHub Pages, etc.).
