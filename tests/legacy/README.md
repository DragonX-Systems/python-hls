# Legacy Tests

These scripts are retained as historical regression material. The actively maintained test suite lives directly in `tests/`; legacy scripts are not run as part of the default release test command.
